import { PlaceholderPage } from "./PlaceholderPage";

export function ChartStudioPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Chart Studio"
      items={["Editable titles and labels", "Top N and time bucket controls", "PNG, SVG, and HTML export presets"]}
    />
  );
}
