import { PlaceholderPage } from "./PlaceholderPage";

export function ExceptionAnalyzerPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Exception Analyzer"
      items={["Exception trend", "Root cause grouping", "Stack signature table"]}
    />
  );
}
