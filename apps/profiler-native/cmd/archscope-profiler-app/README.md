# Fyne App Placeholder

The native GUI will live here after the Go profiler core reaches parity for
the profiler-first slice. Keeping the Fyne dependency out of the first POC
lets `go test ./...` run without platform GUI setup.
