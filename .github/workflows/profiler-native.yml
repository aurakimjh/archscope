name: Engine Native (Go + Wails)

on:
  pull_request:
    paths:
      - "apps/engine-native/**"
      - "examples/**"
      - ".github/workflows/profiler-native.yml"
  push:
    branches:
      - main
    paths:
      - "apps/engine-native/**"
      - "examples/**"
      - ".github/workflows/profiler-native.yml"

defaults:
  run:
    working-directory: apps/engine-native

jobs:
  go:
    name: Go test/build (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: true
          cache-dependency-path: apps/engine-native/go.sum

      - name: Linux WebKitGTK headers
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: go vet
        run: go vet ./...

      - name: go build
        run: go build ./cmd/archscope-engine ./cmd/archscope-profiler ./cmd/archscope-profiler-app

      - name: go test
        run: go test ./... -race -count=1

  wails-frontend:
    name: Wails frontend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/engine-native/cmd/archscope-profiler-app/frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/engine-native/cmd/archscope-profiler-app/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
